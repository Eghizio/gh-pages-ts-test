{
  "name": "gh-pages-ts-test",
  "version": "1.0.0",
  "homepage": "https://Eghizio.github.io/gh-pages-ts-test",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:gh-pages": "npm run build && cp .gitignore.deployment .gitignore",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-dev-server --node-resolve --open --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "gh-pages": "^6.2.0",
    "typescript": "^5.3.0"
  }
}
